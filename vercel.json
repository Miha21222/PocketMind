{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix frontend ci && npm --prefix frontend run build && python -c \"import pathlib, shutil; src=pathlib.Path('frontend/dist'); dst=pathlib.Path('backend/frontend_dist'); shutil.rmtree(dst, ignore_errors=True); shutil.copytree(src, dst)\"",
  "functions": {
    "api/index.py": {
      "runtime": "python3.12"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
